@model PortfolioProjectNight.Models.Service

@{
    ViewBag.Title = "CreateService";
    Layout = "~/Views/Admin/Index.cshtml";
}

<form method="post">
    <div class="container">
        <br /><br />
        <div class="alert alert-success">Yeni Servis Bilgisi Ekleme</div>
        <label>Başlık</label>
        @Html.TextBoxFor(x => x.Title, new { @class = "form-control" })
        <br />
        <label>İkon</label>
        @Html.TextBoxFor(x => x.Icon, new { @class = "form-control" })
        <br />
        <label>Açıklama</label>
        @Html.TextAreaFor(x => x.description, new { @class = "form-control" })
        <br />
        <button class="btn btn-primary btn-block text-center">Ekle</button>
        <a href="/Services/ServiceList" class="btn btn-secondary btn-block text-center">Vazgeç</a>
    </div>
</form>